import { ValidarSenhaDirective } from './validar-senha.directive';

describe('ValidarSenhaDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidarSenhaDirective();
    expect(directive).toBeTruthy();
  });
});
